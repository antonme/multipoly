{
  "name": "glm-mcp",
  "version": "0.1.0",
  "description": "Claude Code plugin that exposes GLM 5.1 via MCP for code review and design consultation.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node scripts/glm-mcp.mjs",
    "health": "node scripts/glm-mcp.mjs --health",
    "test": "node --test --test-reporter=spec tests/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
