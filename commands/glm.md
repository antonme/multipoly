---
description: Free-form single-shot prompt to GLM 5.1. Prefer /glm-review or /glm-consult when they fit.
---

Call the `glm_freeform` MCP tool with `{ "prompt": "$ARGUMENTS" }`. Return the response verbatim.
